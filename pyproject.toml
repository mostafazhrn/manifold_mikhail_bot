[project]
name = "mikhail_bot"
version = "0.1.0"
description = "Manifold MikhailTal trading bot"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["mikhail_bot"]
package-dir = {"" = "src"}

