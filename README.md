# Manifold Mikhail Bot

Skeleton repo for a bot that trades only on markets created by `MikhailTal`.

## Run (paper mode)
```
pip install -r requirements.txt
cp .env.example .env
python examples/run_bot.py --paper
```
