Contributions welcome.

- Run tests with `pytest`.
- Follow code style with `black` and `ruff`.
- Open PRs against `main`.
- Suggested PR to upstream `microprediction/manifoldbot`: add helper to fetch markets by creator and include an example strategy that filters by username.